# 明日计划：2026-08-13（豌豆生长收尾 + 方块更新机制）

> 计划：2026-08-12 晚制定　|　昨日工作见 `docs/status/WORKLOG_2026-08-12.md`，
> 长期任务见 `docs/status/TODO_LIST.md`，方块更新机制规划草稿见 WORKLOG §6（待复核）。

---

## 一、待玩家操作（先做）

| 项 | 内容 | 说明 |
|---|---|---|
| 1 | **复核方块更新机制规划草稿**（WORKLOG_2026-08-12.md §6） | 确认后今天实施；这是明日主线 |
| 2 | 提供豌豆**阶段 3 花/荚贴图**坐标（如 pos_PeaPlantFlower_top/bottom 等） | 只改 `PeaTextures.cs` 坐标，两格形态已就绪 |

---

## 二、待实施任务（按优先级）

### P0：随机刻 20Hz Play Mode 验证
- **状态**：代码已改（`PEA_GROWTH_TICK_INTERVAL=0.05f` + 每 tick 3 次 + `World.Update` while 补 tick），未进编辑器验证。
- **验证点**：豌豆生长节奏与 1Hz 时代完全一致（单阶段 ≈ 205s、全熟 ≈ 10 分钟）；低帧率下不丢 tick；无性能异常（20 次/秒 × 每 chunk 3 次抽位）。

### P1：方块更新机制（待玩家复核规划后实施）
- **Step A：随机刻泛化**（行为零变化）——`TickPeaRandomTicks` 豌豆逻辑迁入 `BlockUpdateCenter.DispatchRandomTick` 类型分派；Play Mode 对比确认生长节奏不变。
- **Step B：BlockUpdate 通知 + 联动迁移**（核心）——运行时写入收敛 `store.SetBlock`（内部变化检测 + 本位置/6 邻居通知，`BlockUpdateSource { Place, Break, StateChange, NeighborChanged }`，递归深度上限 256）；豌豆破坏联动从 `BlockInteraction` 特判迁入更新系统；生成期写入 suppressUpdate。
- **Step C：ScheduledTick 计划刻**（预留，可选）——tick 计数器 + 按 chunk 待执行列表；暂不接方块或做沙子下落演示。
- **配套**：规划复核通过后先存 `docs/design/BLOCK_UPDATE_SYSTEM.md`，实施后更新 TODO_LIST/AGENTS/WORKLOG。
- **复用会话**：fix-2（世界集成/随机刻/两格高全部上下文）。

### P2：豌豆阶段 3（开花结果）贴图接入
- 玩家提供花/荚贴图坐标后：`PeaTextures.cs` 加对应 cell 常量（如 `PlantFlowerTopCell`），阶段 3 顶部/底部换贴图；更新文档。

### P3：基因系统路线图（TODO_LIST.md，可选推进）
- Step 1g：Play Mode 验证存档 v2（种豌豆 → 结荚 → 重启 → 阶段与基因保留）——前置依赖 P0 验证。
- 2d/2e/2f 剩余（TagPresetConfig / BackpackSaver / TagEditorWindow）/ 2g-2i / Step 2 育种。

---

## 三、明确不做（本期范围外）
- 存档计划刻/更新队列（等方块更新机制落地并出现需求再扩展存档格式）
- 红石级高频更新、后台线程更新（延续主线程纪律）
- 存档格式变更（方块更新机制为纯机制重构，v3 格式不动）
