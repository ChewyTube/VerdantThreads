# 工作日志：2026-08-13（全项目代码审查 + 豌豆阶段 3 豆荚贴图接入（绿/黄））

> 记录：2026-08-13　|　关联：`docs/status/PLAN_2026-08-13.md`（P2 阶段 3 贴图接入，本次完成）、
> `docs/status/WORKLOG_2026-08-12.md`（贴图迁移/两格高/20Hz）、`AGENTS.md`（贴图映射说明已同步）。
> 会话主线：全项目代码审查 → 贴图命名规范 → 绿荚接入 → 黄荚接入。

---

## 一、今天做了什么

### 1. 全项目代码审查（oracle 深度审查 + 逐条核实）

> 状态：**已完成**（审查报告交付于会话，未落盘为独立文档；结论经 orchestrator 对照源码逐条核实）。

- **审查范围**：`Assets/Scripts` 全部业务代码（World/Chunk/Block/Genetics/UI/Player/Inventory），FastNoiseLite 跳过。
- **输出**：按严重度分组的审查报告（高 2 / 中 8 / 低 6），每条带 `文件:行号` 定位。
- **核实修正**：oracle 初始报告的高危 #1「随机刻枚举期间按需创建 chunk → 集合修改异常」经核实为**误报**——
  `TryEnsurePlantTop`（BlockUpdateCenter.cs:172-194）在跨 chunk 情形先 `GetChunkBlocks` 判空，
  空区块标记 chunk 不在 `world` 字典（返回 null）即提前 return，不会在 `foreach` 迭代中走到 `CreateEmptyVoxelChunk`。
  降级为潜在契约风险（`ForEachLoadedChunk` 遍历期间不可改集合，未来方块需注意）。
- **确认的高危问题**（供后续排期）：
  1. 空中放置豌豆不掉落——`OnBlockWritten` 本位置用旧块分派，`PeaStem + Place` 分支（BlockUpdateCenter.cs:253）死代码；
  2. 存档命中路径泄漏池化块数组（TerrainGenerator.cs:48-53）；
  3. 跨 chunk 生成期写入在目标卸载后静默丢弃 → 边界树冠永久残缺（ChunkStreamer.cs:169-171）；
  4. `World.Update` tick 追赶 while 循环无上限（World.cs:80-85）；
  5. 豌豆种子基因形同虚设（`ItemInstance.Genome` 零调用点，BlockInteraction.cs:106）。
- **中/低危**：mesh 上传排帧预算末位可能饿死、`World.cs:39` 闭包捕获未赋值 `streamer`、放置高度魔法数 `CHUNK_SIZE*16`、
  背包打开不暂停生长（语义需确认）、死代码群（`EmptyCheck`/`GetUintBlockType`/`GetUVIndex`/`DataBuffer`/`Crossover`/`Mutate`）、
  `MeshData.cs:16` 过时注释（512 应改 768）、UI 图标 UV 公式重复、`VoxelChunk.Start` 硬抛异常、Saver 写失败运行期静默。

### 2. 豌豆阶段 3 豆荚贴图命名规范建议（会话，无代码改动）

- 建议沿用既有 `pos_` 前缀约定：`pos_PeaPod{颜色}{花位}_{top|bottom}.png`（颜色在前，与 PeaTraits 位点顺序一致：位点4 豆荚色 → 位点5 花位置）。
- 玩家最终落盘文件名为 `PeaPlantShort_Pod_{Axillary|Terminal}_{Green|Yellow}_{top|bottom}.png`（"结果"即豆荚，用 Pod 语义）。

### 3. 绿色豆荚贴图接入（main.py + 图集重生成 + 贴图映射切换）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新导入 Atlas.png）。

- `main.py`：加载 4 张绿荚贴图，占整列 4，格位 `(4,0)/(4,1)` 腋绿、`(4,4)/(4,5)` 顶绿；`(4,2)/(4,3)、(4,6)/(4,7)` 预留给黄荚。
- 重新生成 `Assets/Atlas.png`（768×768 RGBA），桌面副本同步；逐格验证 25 素材中心像素与图集格位一致。
- `PeaTextures.GetFlowerCells` **映射切换**：阶段 3 从「花色（位点2 紫/白）× 列3」切到「豆荚色（位点4 绿/黄）× 列4」，
  公式 `row = (腋生?0:4) + (绿色?0:2)`；黄荚未画时隐性黄回退无荚植株贴图 (2,5)/(2,4)（防隐形）。
- `AGENTS.md` 阶段 3 贴图映射说明同步更新。

### 4. 黄色豆荚贴图接入（main.py + 图集重生成 + 回退分支移除）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新导入 Atlas.png）。

- Resources 新增 4 张黄荚贴图（`Axillary_Yellow_buttom.png` 文件名拼写为玩家原样 "buttom"，按实际文件名引用）。
- `main.py`：加载 4 张黄荚贴图，填入预留格位 `(4,2)/(4,3)` 腋黄、`(4,6)/(4,7)` 顶黄。
- 重新生成 `Assets/Atlas.png`，桌面副本同步；逐格验证 29 素材中心像素与图集格位一致（ALL OK）。
- `PeaTextures.GetFlowerCells` **移除黄荚回退分支**：绿/黄 4 表型全部由公式覆盖，`GetFlowerCells` 恢复为纯映射。
- `AGENTS.md` 同步：移除"黄荚预留/回退"措辞。

**阶段 3 豆荚格位最终映射**：

| 表型 | 格位 |
|---|---|
| 腋生·绿荚 | (4,0)/(4,1) |
| 腋生·黄荚 | (4,2)/(4,3) |
| 顶生·绿荚 | (4,4)/(4,5) |
| 顶生·黄荚 | (4,6)/(4,7) |

### 5. 代码审查高危项修复（5 项，fix-1 实施 + 逐处核实）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- **① 空中放置豌豆不掉落**（`BlockUpdateCenter.cs:211-213`）：`OnBlockWritten` 本位置原本只用旧块分派（放置时旧块=Air 走 default），
  修复为 `source == Place` 时**再用新块分派一次本位置**，使空中种植命中 PeaStem 的支撑检查分支（`DropPeaPlant` 掉落）。
- **② 存档命中路径泄漏池化块数组**（`TerrainGenerator.cs:50-55`）：`TryLoadVoxelChunk` 读回后 `Array.Copy(loaded, blocks, loaded.Length)`
  拷入池化数组再构造 `VoxelChunkData`，池数组进入正常归还流。
- **③ 跨 chunk 生成期写入在目标卸载后静默丢弃**（`ChunkStreamer.cs` 新增 `FlushPendingWritesForChunk` + 卸载循环 :326 调用）：
  卸载某 chunk 前先排空队列中目标为该 chunk 的 pendingBlocks/pendingTileWrites（此刻目标仍在 world 字典，写入成功随卸载存档），
  防止边界树冠/豌豆 tile 永久残缺。
- **④ tick 追赶 while 无上限**（`Constants.cs:26` 新增 `MAX_GAME_TICKS_PER_FRAME = 5`；`World.cs:80-89` 有上限循环 + 超限清零积压）：
  防"卡顿→追赶更多 tick→更卡"正反馈。
- **⑤ 豌豆种子基因形同虚设**（`BlockInteraction.cs:106`）：`Genome.Random()` → `current.Genome ?? Genome.Random()`，
  优先使用物品携带基因（当前种子 Genome 仍为 null，行为不变；为收获/遗传系统铺路）。
- 修复说明：贴图随机旋转方案已落盘 `docs/design/TEXTURE_ROTATION.md`（规划中，本次未实施，用户要求后做）。

### 6. 贴图随机旋转实施（fixer 实施 + 逐处核实）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- **方案**：`docs/design/TEXTURE_ROTATION.md` 定稿落地。旋转 = UV 角点循环位移（几何不动），朝向由 `Hash(seed, 世界坐标)` 确定性决定。
- **`MeshData.cs`**：`AddFace`/`AddPeaQuadCell`/`AddPeaQuad` 增加 `int rotation = 0` 参数；`GetFaceUVs` 与豌豆 uv 数组做 `rotation & 3` 次循环左移（保持四边形环绕方向，不翻转；采样区不越出 16×16 内容区）。
- **`TextureRotation.cs`（新建）**：`GetRotation(seed, wx, wy, wz)` golden-ratio 哈希（uint 环绕乘法，后台线程安全）；`ShouldRotateFace` 白名单**仅草方块顶面（Grass+Up）**。
- **`ChunkMeshBuilder.cs`**：`MeshBuildData.Seed` 字段；`CreateSnapshot` 增加 `int seed`；PeaStem 用本格世界坐标 `(xw,yw,zw)`，PeaPlantTop 用底部格坐标 `(xw,yw-1,zw)`（同株两格朝向一致、跨 chunk 安全）；`TryAddFace` 按白名单算 rot。
- **`ChunkStreamer.cs` / `World.cs`**：构造函数增加 `int seed`，`World.seed(985211) → ChunkStreamer → CreateSnapshot → MeshBuildData.Seed` 传递链完整。
- 面剔除、tile 基因、存档逻辑零改动；`AGENTS.md` 与 `TEXTURE_ROTATION.md` 状态同步为已实施。
- 验证要点（Play Mode）：豌豆顶/底格朝向一致且跨 chunk 一致；植株间 4 向随机分布；草顶面相邻格不再对齐；卸载重载后朝向不变；无采样错位。

### 7. 豌豆旋转禁用（用户反馈：十字面片旋转视觉异常）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- 用户反馈：俯视豌豆时旋转表现为"上下翻转"而非逆时针旋转（十字面片为垂直双面片，UV 旋转在俯视下读作翻转/镜像，与草顶面水平面的旋转观感不同）。
- `ChunkMeshBuilder.cs`：PeaStem（:114）与 PeaPlantTop（:136）的 `rot` 固定为 `0`，豌豆不再旋转；草方块顶面旋转保留（`TryAddFace` :168 白名单不变）。
- 恢复方式：把两处 `int rot = 0;` 改回 `TextureRotation.GetRotation(d.Seed, xw, yw, zw)`（底部格）/ `(d.Seed, xw, yw - 1, zw)`（顶部格）即可，代码注释已写明。
- `AGENTS.md` 旋转说明同步（豌豆旋转禁用 + 恢复指引）。

### 8. 豌豆旋转重做（方案 B：单面朝向 quad，fixer 实施 + 逐处核实）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- 背景：用户确认需求为「豌豆有可见正面，随机朝东南西北，俯视不翻转」。几何分析结论：垂直面片绕 Y 轴 180° 与自身重合 → N/S 同平面、E/W 同平面，任何垂直面片几何最多 2 种朝向；十字面片四面同观无正面可言。选定方案 B（单面 quad 朝向 4 向）。
- **`MeshData.cs`**：新增 `AddPeaQuadFacing(x, y, z, cell, facing)`（:130-148）——一片垂直 quad 居中于格子，facing 0/1/2/3 = 北/东/南/西 四向顶点表（已核对绕序：正面贴图正立不镜像）；UV 恒 `v=world-up`（**不做循环位移 → 永不翻转**）；复用双面 `AddQuad`（法线朝上，光照均匀）。
- **`ChunkMeshBuilder.cs`**：PeaStem（:114）`rot = TextureRotation.GetRotation(d.Seed, xw, yw, zw)`；PeaPlantTop（:137）`rot = GetRotation(d.Seed, xw, yw - 1, zw)`（与底部格同 hash → 同株同朝向）；三个 stage 分支与顶部格全部改调 `AddPeaQuadFacing`。面剔除、tile 基因、存档零改动。
- **已知限制**（用户已确认接受）：① 几何上仅 2 种平面（N/S、E/W），朝向差异靠正反面镜像体现；② 朝向与视线垂直的豌豆 edge-on 变薄；③ 正面差异依赖贴图左右不对称（阶段 3 花/豆荚明显，阶段 0/1/2 若贴图对称则差异弱）。
- 旧十字版 `AddPeaQuadCell`/`AddPeaQuad` 暂留作回退（验证后清理）。
- 验证要点（Play Mode）：豌豆正面随机朝向、永不翻转；同株顶/底格朝向一致；转视角观察 edge-on 影响程度；若 edge-on 不可接受 → 回退对角线单面（永不 edge-on）或方案 A（不旋转）。

### 9. 豌豆渲染回退：十字面片 + UV 旋转（用户否决方案 B 单面 quad）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- 用户反馈：方案 B（单面 quad）观感不对，豌豆应为十字面片；要求恢复十字面片渲染并**保留旋转**。
- `ChunkMeshBuilder.cs`：PeaStem 三个 stage 分支与 PeaPlantTop 全部改回 `AddPeaQuadCell`/`AddPeaQuad`（十字面片）；`rot` 保留 `GetRotation` 哈希（底部格 `(xw,yw,zw)`、顶部格 `(xw,yw-1,zw)`）→ 十字面片 + UV 循环位移旋转。
- 已知观感：垂直面片 UV 旋转在俯视下读作翻转/镜像（用户此前否决过，本次明确要求十字面片 + 旋转，接受此观感）。
- `MeshData.AddPeaQuadFacing`（方案 B 单面版）暂留作回退，验证后清理。
- 验证要点（Play Mode）：豌豆十字面片渲染恢复；旋转随机（4 向哈希）；同株顶/底格朝向一致；俯视翻转/镜像观感是否可接受。

### 10. 豌豆旋转取消（用户要求：取消豌豆所有旋转）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- 用户要求取消豌豆的所有旋转（历程：十字面片 UV 旋转 → 方案 B 单面 → 回退十字面片 → 最终决定豌豆不旋转）。
- `ChunkMeshBuilder.cs`：PeaStem（:114）与 PeaPlantTop（:136）的 `rot` 固定为 `0`；草方块顶面旋转保留（`TryAddFace` 白名单不变）。
- 恢复方式：把两处 `int rot = 0;` 改回 `TextureRotation.GetRotation(d.Seed, xw, yw, zw)`（底部格）/ `(d.Seed, xw, yw - 1, zw)`（顶部格）即可，代码注释已写明。
- `MeshData.AddPeaQuadFacing`（方案 B 单面版）仍为死代码暂留。
- `AGENTS.md` 旋转说明同步（豌豆旋转禁用）。

### 11. 豌豆旋转改为 XZ 几何旋转（用户确认方案）

> 状态：**已完成**；未进编辑器 Play Mode 验证（需 Unity 重新编译后进入）。

- 背景：UV 循环位移旋转在垂直面片上读作翻转/镜像（用户否决）；方案 B 单面 quad 也被否决。用户提出：十字面片几何绕 Y 旋转 + 不对称贴图 → 花/豆荚的可见侧随旋转换向（4 态互不相同），且贴图始终 v=world-up 直立不翻转。
- `MeshData.cs` `AddPeaQuadCell`：UV 循环位移改为顶点 XZ 几何旋转（新增 `RotateQuadXZ` 辅助，绕格心 (x+0.5, z+0.5) 绕 Y 轴，Unity 旋转约定；顶点偏移恒为 ±0.5 → 坐标精确无浮点误差）；UV 数组不动。
- `ChunkMeshBuilder.cs`：PeaStem（:114）与 PeaPlantTop（:136）恢复 `TextureRotation.GetRotation` 哈希调用（底部格 `(xw,yw,zw)`、顶部格 `(xw,yw-1,zw)` 同株同向）。
- 草方块顶面 UV 旋转不受影响（水平面 UV 旋转观感正确）。
- `AddPeaQuadFacing`（方案 B 单面版）仍为死代码暂留。
- `AGENTS.md` / `TEXTURE_ROTATION.md` 同步。

### 12. 高茎豌豆（位点 6 茎高度 → 三格高植株 + 新图集）

> 状态：**已完成（代码+图集）**；未进编辑器 Play Mode 验证（需 Unity 重新编译 + 新 Atlas.png 导入）。

- 用户画好了全部高茎豌豆贴图（`PeaPlantTall_*`：基础三格 + 花/荚 4 变体 × 3 格），要求改 main.py 生成新图集并改 C#。
- **设计**：基因位点 6 = 茎高度（孟德尔 7 性状之一，此前未用）。高茎显性 → 阶段 2/3 为**三格高**（底 PeaStem + 新中部格 PeaPlantMiddle=10 + 顶 PeaPlantTop）；矮茎隐性 → 两格高（现状）。
- **main.py**：新增高茎 27 张（列 5 = 基础三格 (5,2)/(5,1)/(5,0)；列 6 = 高茎花预留；列 7 = 高茎荚 (7,row)/(row+1)/(row+2)，row=(腋生?0:6)+(色?0:3)）。缺失素材跳过+警告。⚠️ 缺 `PeaPlantTall_Flower_Terminal_White_top.png`（列 6 预留花，渲染用不到；用户补画后重跑即可）。
- **C#**（fixer B1 + 手工补 B2——fix-4 只读未写，已由编排器直接实现）：
  - Block.cs：`PeaPlantMiddle = 10`；`BlockBits.TallMask = 0x4`（状态位 bit2 = 块值 bit18）；`Block.WithTall/IsTallPlant`。
  - PeaTextures.cs：高茎三格常量 + `IsTall` + `GetTallFlowerCells`（列 7）。
  - ChunkMeshBuilder.cs：三分支（底/中/顶）按自身状态 + 底部格基因选贴图；中部/顶部格 rot 用底部格世界坐标 hash（同株同向）；阶段 2 顶部不再提前开花（顺带修复旧缺陷）；`ShouldBeEliminated` 加 PeaPlantMiddle。
  - BlockUpdateCenter.cs：1→2 高茎占 y+1/y+2（PeaPlantMiddle+PeaPlantTop，均带阶段 2 + 高茎位）、矮茎 y+1；2→3 `SyncUpperStage` 同步中部/顶部阶段；破坏联动覆盖中部格（底破→清中+顶；中破→清顶+底退 0；顶破→矮：底退 0 / 高：清中+底退 0）。
  - ChunkStore.cs：`RepairPeaPlants` 按基因补结构（高茎中+顶、矮茎顶）+ 孤儿中部/顶部判定 + 跨 chunk y=0/y=1 补位 + 阶段同步（旧档顶部阶段 0 → 同步）。
- 图集已验证：768×768，列 5-7 有 26 个非空格（27 减 1 缺失）。
- 文档同步：AGENTS.md（豌豆系统、方块位、图集、破坏联动、旋转 key）。

### 13. 豌豆阶段链扩为 5 段：独立开花阶段（修复"没开花直接结果"）

> 状态：**已完成（代码）**；未进编辑器 Play Mode 验证（需 Unity 重新编译）。

- 用户反馈：所有豌豆没开花就结果。诊断：阶段链只有 4 段（StageMask 2 位），阶段 3 = "开花结果"合并态，代码直接渲染**荚**贴图（列 4/7），**花**贴图（列 3/6）从未接线。
- 用户确认：扩到 5 阶段——0 最小苗 → 1 苗 → 2 植株 → 3 **开花**（花贴图）→ 4 **结果**（荚贴图）。
- `Block.cs`：`StageMask 0x3→0x7`（3 位，bit16-18）；⚠️ **TallMask 从 0x4 移到 0x8**（状态位 bit3 = 块值 bit19）——否则阶段 4（0b100）会误置高茎标志。旧档兼容：阶段值 0-3 读回不变；中部/顶部格的阶段与高茎标志由加载修复重写，位布局迁移安全。
- `PeaTextures.cs`：方法拆分——阶段 3 `GetFlowerColorCells`/`GetTallFlowerColorCells`（列 3/6，花色位点2 × 花位位点5）；阶段 4 `GetPodCells`/`GetTallPodCells`（列 4/7，荚色位点4 × 花位位点5）；旧 `GetFlowerCells`/`GetTallFlowerCells` 更名删除。
- `ChunkMeshBuilder.cs`：三分支（底/中/顶）阶段 3 取花贴图、阶段 4 取荚贴图、阶段 2 基础贴图。
- `BlockUpdateCenter.cs`：生长判停 `stage >= 3` → `>= 4`（阶段 3→4 也走 SyncUpperStage 同步）。
- 全熟时间：3 段 10 分钟 → 4 段约 13.7 分钟。
- 文档同步：AGENTS.md / GROWTH_RANDOM_TICK.md。

---

## 二、下次开工入口

| 项 | 内容 | 备注 |
|---|---|---|
| 1 | Play Mode 验证：阶段 3 豆荚贴图（绿/黄 × 腋/顶 四表型按基因正确显示） | 需 Unity 重新导入 `Assets/Atlas.png` |
| 2 | Play Mode 验证：高危项修复（空中种豌豆掉落、边界树冠完整、tick 上限）+ 贴图旋转（草顶面相邻格不再对齐、重载不变；豌豆不再旋转） | 需 Unity 重新编译 |
| 3 | 背包打开是否应暂停生长（语义确认） | 需玩家决策 |
| 4 | 基因系统路线图剩余（Step 1g 验证 / 2d-2i / Step 2 育种） | 见 TODO_LIST.md |

**可复用会话**：oracle（代码审查全部上下文，含已核实结论）——后续高危项修复设计首选复用；fix-2（世界集成/随机刻/两格高上下文）——机制修复首选复用；fix-3（贴图旋转实施上下文，含方案 B 单面 quad 与回退）——渲染层改动首选复用。
