# 工作日志：2026-08-10（豌豆批2 设计与文档整理）

> 记录：2026-08-10 深夜（环境日期 2026-08-11，熬夜跨日会话）。
> 目的：供明天开工快速恢复上下文。当前任务见 `docs/status/TODO_LIST.md`，批2 设计定案见 `docs/design/TAG_SYSTEM.md`。

---

## 一、今天做了什么

### 0. 讨论搭档提示词

- 为"项目概况讨论搭档"agent 编写了交接提示词（角色定位、项目速览、必读文档、分工约定、纪要输出模板），并交付作者供其复制使用。

### 1. 批2「豌豆记录与标签系统」任务接收与确认

- 收到实现任务书（豌豆遗传玩法——记录与标签系统，含 7 项已定决策 + 4 项待确认技术问题）。
- 通读 `PROJECT_UNDERSTANDING.md` / `GAME_DESIGN.md` / 代码现状，确认关键缺口：
  - ❌ 无背包/物品系统（"豌豆进背包"需从零建）
  - ❌ 无任何 UI 基础设施（无 Canvas/UGUI，仅 OnGUI 准星）
  - ❌ `Genome` struct 未实现（Step 1b 未做）
  - ❌ `ItemInstance` 类型不存在
  - ⚠️ 右键当前=放置方块，与"右键采摘"冲突，需改交互分发
  - ✅ 现有豌豆占位贴图已满足"表现型植株可见（占位颜色）"，无需额外视觉工作

### 2. 四项技术问题确认（作者拍板）

| 问题 | 结论 |
|---|---|
| 标签存储位置 | `ItemInstance` 实例挂载（phenotypeTags / genotypeTags 两个 List），不做管理器映射 |
| UI 底座 | **IMGUI（OnGUI）** 最小背包 + 标签编辑窗（零场景依赖） |
| 预设配置 | `TagPresetConfig : ScriptableObject`（CreateAssetMenu）+ 代码内建默认兜底 |
| 背包存档 | **本次就做**：NBT 式极简 tag 树，`world_saves/backpack.dat`，与 genome 一起序列化 |
| Genome 来源 | 本次实现 `Genome` struct；采摘每株随机生成一次、同次产物共享（tile 落地后改读 tile） |

### 3. 采摘产物方案确认（作者拍板）

按成熟度分两类产物：
- 苗期（stage 0）：不可采，提示
- 开花期（stage 1）：**1 个「青嫩豆荚」**（种子性状未定型，不可观察）
- 结荚期（stage 2）：**3~5 个「成熟豆荚」**（种子圆/皱、黄/绿可观察；数量保证实验样本）

### 4. 落盘批2 设计定案

- 新建 `docs/design/TAG_SYSTEM.md`：7 项已定决策 + 技术确认结论 + 实现清单（10 新增 + 3 修改）+ 约束。

### 5. 文档规范化整理（全部完成，无数据丢失）

- 命名统一 `UPPER_SNAKE_CASE.md`：
  - `todolist.md` → `TODO_LIST.md`
- 已完成/过期归档（原文保留，仅加归档头）：
  - `TODO.md` → `archive/TODO_HISTORY.md`
  - `PROJECT_STATUS.md` → `archive/PROJECT_STATUS_2026-08-09.md`（过期快照）
  - `AGENTS.md.bak` → `archive/AGENTS_OLD.md`
  - `REVIEW.md` / `VIEW_DISTANCE_PLAN.md` / `PLAN_2026-08-10.md` 补归档状态头
- 数据保全：遗留开放项 P3-13（树外观瑕疵，低优先级）迁移至 `TODO_LIST.md` 其他待办
- 交叉引用全部修复；`docs/README.md` 索引重写（补入漏列的 TAG_SYSTEM.md）；根 `README.md` 补全为仓库入口

### 6. 批2 任务表

- `TODO_LIST.md` 新增批2 步骤表 2a~2i（Genome → 性状表 → ItemInstance/Backpack → 预设配置 → BackpackSaver → UI 三窗 → Validator → 交互集成 → Play Mode 验证）。

---

## 二、明天开工入口

**批2 实现（`docs/status/TODO_LIST.md` 步骤 2a~2i，设计见 `docs/design/TAG_SYSTEM.md`）：**

| 步骤 | 内容 |
|---|---|
| 2a | `Genetics/Genome.cs`：uint32 打包 7 位点×2 等位×2bit + 访问器/Crossover/Mutate/Random |
| 2b | `Genetics/PeaTrait.cs`：7 对性状定义表（GDD §6 顺序：圆粒/皱粒→黄子叶/绿子叶→紫花/白花→豆荚饱满/皱缩→绿豆荚/黄豆荚→花腋生/顶生→高茎/矮茎） |
| 2c | `Inventory/ItemInstance.cs` + `Inventory/Backpack.cs`（非堆叠列表） |
| 2d | `Inventory/TagPresetConfig.cs`：14 表现型预设 + 4 基因型预设 |
| 2e | `Save/BackpackSaver.cs`：NBT 式 tag 树 → `world_saves/backpack.dat` |
| 2f | `UI/BackpackWindow.cs`（F）+ `UI/TagEditorWindow.cs`（双分区异色）+ `UI/StandardizeWindow.cs`（F9 调试向导） |
| 2g | `Genetics/GenomeValidator.cs`：`ValidateGenotypeTag` + 多性状重载（预留接口） |
| 2h | `BlockInteraction.cs` 右键分发采摘 + `World.cs` 装配 Backpack/Saver + `Constants.cs` 常量 |
| 2i | Play Mode 验证：种→结荚→采摘→F 标签→F9 标准化→重启看背包存档 |

**待作者决策（开工前）：**
- 是否派发 @fixer 实现（此前作者指示"暂不派发"，先审阅 `docs/design/TAG_SYSTEM.md`）。

---

## 三、资源备忘

- 设计定案：`docs/design/TAG_SYSTEM.md`
- 当前任务：`docs/status/TODO_LIST.md`
- 文档索引：`docs/README.md`
- 项目认知：`docs/PROJECT_UNDERSTANDING.md`
- 历史归档：`docs/archive/`（6 个文件）
- 豌豆批1 已完成（数据+占位图+十字面片渲染），Genome/tile/生长 tick 属批2 及后续路线图
